# Plan — Maidbook

> Mirror of the Plan sub-page (push-only). v0.3.1 is shipped; the next cycle is v0.4.

## Shipped

- [x] (t:v030) **v0.3.0** — build-artifact purge, agent-tools browser, scheduled clean (cron), lifetime analytics, async deletion, 7 health modules, full TUI overhaul.
- [x] (t:v031) **v0.3.1** — docs patch (README rewrite for all v0.3 features). On PyPI. 110/110 tests green.

## Next cycle — v0.4

- [ ] (t:v04) v0.4 milestone — roadmap in `README.md`. Starting it = bump `Version` to `0.4.x`, which **reopens this item to Spec** (version-reopen logic) to define v0.4 scope, then Plan → Build → Review → Done.

> While v0.3.1 is the shipped release with no open work, this item sits at **Done**. The version bump is the trigger that reactivates it.
